// ConsoleApplication1.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
using namespace std;
int main()
{
	std::cout << "Hello World!\n";
	std::cout << "this is vry strange.\n";
	std::cout << "\nwell then.\n";

	/*
	IF ELSE STATMENTS
		int score;
		std::cout << "\n\n\nEnter Your Score: ";
		std::cin >> score;
		if (score >= 1000)
		{
			std::cout << "\nCONGRATUALTIONS!! You celebrated";
		}
		else if (score >= 500)
		{
			std::cout << "\nPretty Epic.";
		}
		else if (score >= 250)
		{
			std::cout << "\nNot very epic sir.";
		}
		else if (score >= 0)
		{
			std::cout << "\nAbsolute dog shit.";
		}
	*/
	/*
	SWITCH STATMENTS
	int value;
	std::cout << "pick a number 1 - 4" <<endl;
	std::cin >> value;
	switch (value)
	{
	case 1:
		std::cout << "\nYou picked 1";
		break;
	case 2:
	std:cout << "\nYou picked 2";
		break;
	case 3:
		std::cout << "\nYou picked 3";
		break;
	}
	*/
	/*
	WHILE LOOPS
	char playAgain = 'y';
	while (playAgain == 'y')
	{
		std::cout << "\nthis was fun!";
		std::cout << "\nPlay again? (y/n)";
		std::cin >> playAgain;
	}
	std::cout << "\nOk fuck wad see ya in the trenches.";
	return 0;
	--------------------------------------------------------------------------------------------------------
	*/
	/*
	DO STATMENTS
	char again;
	do
	{
		std::cout << "\nThis was fun!";
		std::cout << "\nPlay Again? (y/n)";
		std::cin >> again;
	} while (again == 'y');
	std::cout << "\nFine you fuck tard, suit yourself.";

			return 0;
	*/
	/*
	CONTINUE AND BREAK FUNCTIONS IN LOOPS
	int count = 0;
	while (true)
	{
		count += 1;
		if (count > 10)
		{
			break;
		}
		if (count == 5)
		{
			continue;
		}
		cout << count << endl;
	}
	*/

	/*
	LOGICAL OPERATORS
	*/

	string username;
	string password;
	bool x;
	std::cout << "\n\tHello. Please enter your Username and Password.";
	do
	{
		std::cout << "\n\nUsername: ";
		std::cin >> username;
		std::cout << "\nPassword: ";
		std::cin >> password;
		if (username == "Joe" || "joe" && password == "nou")
		{
			x = true;
			std::cout << "\nLoggin successful!";
		}
		else {
			x = false;
			std::cout << "\n\tPlease try again";
		}
	} while (x == false);
}




// Run program: Ctrl + F5 or Debug > Start Without Debugging menu
// Debug program: F5 or Debug > Start Debugging menu

// Tips for Getting Started: 
//   1. Use the Solution Explorer window to add/manage files
//   2. Use the Team Explorer window to connect to source control
//   3. Use the Output window to see build output and other messages
//   4. Use the Error List window to view errors
